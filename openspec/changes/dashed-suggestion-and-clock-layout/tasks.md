## 1. Implementation
- [x] 1.1 Add `border: 2px dashed rgba(255,255,255,0.3)` to `#suggestionTile`.
- [x] 1.2 Update `.today` CSS: remove `flex-shrink: 0`, allow wrapping.
- [x] 1.3 Update `.clock` CSS: ensure `flex-shrink: 0`, `text-align: right`.
- [x] 1.4 Update `.side-bottom` alignment to `flex-end` to align bottom of clock with bottom of date (or wrapped date).