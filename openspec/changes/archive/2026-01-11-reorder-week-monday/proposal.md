# Change: Reorder Week to Start on Monday

## Why
The user prefers the week to start on Monday instead of Sunday for the menu display.

## What Changes
- Reorder the `MENU` array in `index.html` to place Monday at the first index.

## Impact
- Affected specs: `menu-display`
- Affected code: `index.html`
