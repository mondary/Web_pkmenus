## 1. Implementation
- [x] 1.1 Update `fetchImage750g` to return `Promise<string[]>`.
- [x] 1.2 Update `renderMenu` to store image lists and handle the initial image display.
- [x] 1.3 Add a click event listener to tiles to cycle through the image list.