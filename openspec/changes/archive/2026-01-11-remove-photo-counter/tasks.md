## 1. Implementation
- [x] 1.1 Hide `.photo-meta` in CSS (since both children `slInfo` and `photoCounter` are now unwanted).
- [ ] 1.2 Remove logic updating `photoCounter` in `index.html` (optional cleanup).