# Change: Remove Photo Counter

## Why
The user requested to remove the "3 / 3" (photo counter) display, further simplifying the sidebar UI.

## What Changes
- Hide or remove the photo counter element (`#photoCounter`) and its container (`.photo-meta`) if empty.

## Impact
- Affected specs: `menu-display`
- Affected code: `index.html`
