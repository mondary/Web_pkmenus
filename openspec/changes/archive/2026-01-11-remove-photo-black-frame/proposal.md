# Change: Remove Photo Black Frame

## Why
The user wants to remove the inner black background/padding around the photo slideshow, leaving only the yellow sidebar background and the photo itself.

## What Changes
- Remove background color and padding from `.side-photo` and `.frame` classes in CSS.

## Impact
- Affected specs: `menu-display`
- Affected code: `index.html`
