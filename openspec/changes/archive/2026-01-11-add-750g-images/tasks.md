## 1. Implementation
- [x] 1.1 Add `fetchImage750g(query)` function using a CORS proxy (e.g., allorigins.win).
- [x] 1.2 Update `renderMenu` to include an image element in the tile structure.
- [x] 1.3 Implement the async loading logic to fetch images after the menu renders.
- [x] 1.4 Add CSS to style the image within the tile (e.g., as a background or thumbnail).