## 1. Implementation
- [x] 1.1 Update `handleTileClick` to save the selected image URL to `localStorage`.
- [x] 1.2 Update `renderMenu` image loading callback to check `localStorage` for a preferred URL and set the initial index accordingly.
- [x] 1.3 Update `updateSuggestion` image loading logic similarly.