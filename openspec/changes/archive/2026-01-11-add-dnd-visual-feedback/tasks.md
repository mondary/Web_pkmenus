## 1. Implementation
- [x] 1.1 Add CSS rules for `.tile.is-dragging` and `.tile.is-drag-over`.
- [x] 1.2 Update `handleDragStart` to add `is-dragging`.
- [x] 1.3 Implement `handleDragEnter` and `handleDragLeave` to toggle `is-drag-over`.
- [x] 1.4 Update `handleDrop` and `handleDragEnd` to clean up classes.