## 1. Implementation
- [x] 1.1 Update CSS for `.tile` (flex-end alignment), `.tile-day` (top-left), and `.tile-title` (large font).
- [x] 1.2 Add CSS for `.is-today` (visual highlight).
- [x] 1.3 Update `renderMenu` in `index.html` to calculate the current day name and apply the `.is-today` class.