## 1. Implementation
- [x] 1.1 Add `draggable="true"` to menu tiles in `renderMenu`.
- [x] 1.2 Implement `handleDragStart` to store the index of the dragged item.
- [x] 1.3 Implement `handleDrop` to swap `title` and `details` between two indices in the `MENU` array.
- [x] 1.4 Ensure the UI re-renders correctly after a swap.