# Change: Align Clock with Date

## Why
The user wants to optimize the vertical space in the sidebar by placing the clock on the same line as the date, to the right.

## What Changes
- Update CSS for `.side-bottom` to display children in a row (flexbox).
- Align items vertically so they look balanced.
- Adjust clock margins if necessary.

## Impact
- Affected specs: `menu-display`
- Affected code: `index.html`
