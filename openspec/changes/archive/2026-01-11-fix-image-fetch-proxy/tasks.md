## 1. Implementation
- [x] 1.1 Update `fetchImage750g` in `index.html` to use `https://api.codetabs.com/v1/proxy?quest=`.
- [x] 1.2 Modify the fetch parsing logic to handle raw text response instead of JSON `.contents`.