## 1. Implementation
- [x] 1.1 Add `const SUGGESTIONS` array with a list of potential dishes.
- [x] 1.2 Update `renderMenu` to append a "Suggestion" tile at the end of the grid.
- [x] 1.3 Create `updateSuggestion()` function to pick a random suggestion and update the tile (text + image).
- [x] 1.4 Set up a `setInterval` to run `updateSuggestion` every hour (3600000 ms).